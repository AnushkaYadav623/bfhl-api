{
  "name": "bfhl-api",
  "private": true,
  "version": "1.0.0",
  "description": "BFHL API — Spring Boot (source) + Vercel serverless (hosting)",
  "scripts": {
    "test:vercel": "node --test api/bfhl.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.0"
  }
}
